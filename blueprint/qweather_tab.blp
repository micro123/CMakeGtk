using Gtk 4.0;

Box root {
    orientation: vertical;
    // 当前天气情况
    Box {
        orientation: horizontal;
        // 天气图标
        Image {
            width-request: 320;
            height-request: 320;
            pixel-size: 8;
            resource: "/dashboard/icons/100.png";
        }
    }

    Box {
        orientation: horizontal;

        // $InfoRow {
        //     label: "测试";
        //     value: "天河区";
        // }
    }
}
